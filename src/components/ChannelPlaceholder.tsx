// Post-unlock landing for c2. The actual Channel surface (Exile
// present; conversation; calibration-aware system prompt assembly)
// lands in Phase 1 §4. Until then this just confirms the vault is
// unlocked and the encrypted state is reachable.

export function ChannelPlaceholder() {
  return (
    <main className="screen">
      <h1>vault unlocked</h1>
      <p>
        Encrypted state is reachable. The Channel surface lands in
        Phase 1 §4.
      </p>
    </main>
  );
}
